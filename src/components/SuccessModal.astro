<div id="success-modal" class="fixed inset-0 z-[200] flex items-center justify-center px-6 opacity-0 pointer-events-none transition-all duration-500">
    <div class="absolute inset-0 bg-[#155f98]/20 backdrop-blur-md"></div>
    
    <div class="bg-white rounded-[48px] p-10 md:p-16 w-full max-w-lg relative z-10 shadow-2xl border border-white text-center transform scale-90 transition-transform duration-500" id="modal-card">
        
        <div class="relative w-32 h-32 mx-auto mb-8">
            <div class="absolute inset-0 bg-[#feb300]/20 rounded-full blur-2xl animate-pulse"></div>
            <div id="rocket-container" class="relative z-10 flex items-center justify-center h-full">
                <span class="material-symbols-outlined !text-7xl text-[#af101a] animate-bounce">rocket_launch</span>
            </div>
        </div>

        <h2 class="text-3xl font-black text-[#213041] uppercase tracking-tighter mb-4">Feedback Received!</h2>
        <p class="text-slate-500 font-medium mb-10 leading-relaxed text-sm md:text-base">
            Your review has been successfully sent to Teacher Roya. Thank you for helping us improve our galaxy!
        </p>

        <button id="close-modal" class="w-full bg-[#155f98] text-white py-5 rounded-full font-black uppercase text-xs tracking-widest shadow-xl shadow-blue-900/20 active:scale-95 transition-all">
            Continue Journey
        </button>
    </div>
</div>

<style>
    #success-modal.show {
        @apply opacity-100 pointer-events-auto;
    }
    #success-modal.show #modal-card {
        @apply scale-100;
    }
    
    @keyframes rocket-fly {
        0% { transform: translateY(0) scale(1); opacity: 1; }
        100% { transform: translateY(-200px) scale(0.5); opacity: 0; }
    }
    .fly-away {
        animation: rocket-fly 0.8s ease-in forwards;
    }
</style>